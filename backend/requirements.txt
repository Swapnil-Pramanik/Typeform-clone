# Vercel's Python runtime installs from this file.
# Kept in step with pyproject.toml, which is what local development uses.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
alembic>=1.14.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
sqlalchemy-libsql>=0.1.0
